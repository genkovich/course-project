services:
  claude:
    build: .
    volumes:
      - .:/workspace
      - claude-config:/root/.claude
    working_dir: /workspace
    mem_limit: 4g
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    stdin_open: true
    tty: true

volumes:
  claude-config: