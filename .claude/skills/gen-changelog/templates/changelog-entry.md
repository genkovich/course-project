<!-- Curated [Unreleased] block. Use only the categories that have entries —
delete the empty ones. One line per bullet, plain language, no trailing period,
no feat:/fix: prefix. Fewer lines than the raw git log = curation, not a dump. -->

## [Unreleased]

### Added

- <new capability a user can now use, in plain language>

### Changed

- <a change in existing behaviour the reader will feel>

### Fixed

- <a bug that is now fixed, described by what no longer breaks>

<!-- Deprecated / Removed / Security: add a `### <Category>` block only if there
is something to put under it. -->
